# SonarQube Project Configuration
sonar.projectKey=mi_projecto_seguro
sonar.projectName=mi_projecto_seguro

# Project source configuration
sonar.sources=.
sonar.sourceEncoding=UTF-8

# SonarQube server
sonar.host.url=http://localhost:9000

# Authentication (DO NOT COMMIT real tokens)
# Set this via environment variable in your shell/pipeline instead:
#   export SONAR_TOKEN="..."
# and run: sonar-scanner -Dsonar.login=$SONAR_TOKEN
# If you still want to use the property, keep it empty locally:
sonar.login=
